SpawnPoint(float x, float y){
  float_x = transform.position.x;
  float_y = transform.position.y;
  Instantiate(gameObject.Enemy, float_x, float_y);
}

Class Enemy{

  int health = 0;
  SpawnPoint();
}
  
